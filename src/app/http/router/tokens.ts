import * as Router from 'koa-router';
import * as Koa from 'koa';

import IArgs from '../../contracts/routerArgs';
import Tokens from '../../helpers/tokens';

import { User } from '../../models/Users';

export default (router: Router, args: IArgs): void => { 

}